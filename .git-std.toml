scheme = "semver"
strict = true
scopes = ["auto", "repo", "ci", "docs", "actions", "components", "scripts", "tests", "deps"]

[versioning]
tag_prefix = "v"

[[version_files]]
path = "project.yaml"
regex = '^version: "([^"]+)"$'
